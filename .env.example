# Only PRIVATE_KEY is required to launch the agent; everything else falls back to defaults.

# Agent HTTP configuration
# Optional. Port that Bun will listen on locally.
PORT=8787
# Optional. Base URL surfaced in the agent manifest for absolute links.
API_BASE_URL=http://localhost:8787

# Chain configuration
# Optional. RPC endpoint used for ERC-8004 trust interactions.
RPC_URL=
# Optional. Set to true to auto-register the agent identity at startup.
REGISTER_IDENTITY=false
# Optional. Payment network identifier (e.g. base, base-sepolia).
NETWORK=base-sepolia
# Optional unless monetization is enabled. Facilitator API base URL.
FACILITATOR_URL=https://facilitator.daydreams.systems

# Payments (optional)
# Optional. Address that receives x402 payments.
PAY_TO=0xb308ed39d67D0d4BAe5BC2FAEF60c66BBb6AE429
# Optional. Default price (in base units) when no entrypoint price is set.
DEFAULT_PRICE=1000000

# Twitter Space Agent - Per-entrypoint Pricing (in base units, 1 USDC = 1000000)
# Optional. Price for format-twitter-space entrypoint (default: 0.002 USDC = 2000 base units)
PRICE_FORMAT_SPACE=2000
# Optional. Price for summarize-twitter-space entrypoint (default: 0.0015 USDC = 1500 base units)
PRICE_SUMMARIZE_SPACE=1500
# Note: Health check (GET /health) is provided free by agent-kit, no configuration needed

# Twitter Space Agent - Audio Processing Configuration
# Optional. Duration (in minutes) for audio chunks when splitting large files (default: 10)
# Range: 1-30 minutes. Smaller chunks = more parallel processing but more API calls.
# Recommended: 5-15 minutes for optimal balance between speed and cost.
AUDIO_CHUNK_DURATION_MINUTES=10

# Twitter Authentication (Required for Space downloads)
# Required. Twitter cookies in JSON array format. See COOKIE_EXPORT_GUIDE.md for instructions.
TWITTER_COOKIES=

# Secrets
# Required. Used to sign payment requests with the agent wallet.
PRIVATE_KEY=

# OpenAI Configuration (Required for chat functionality)
OPENAI_API_KEY=your_openai_api_key_here

# Database Configuration
# Optional. Path to SQLite database file (default: ./data/database/spaces.db)
DATABASE_URL=./data/database/spaces.db

# Free API Configuration
# Optional. Port for free HTTP API server (default: 3001)
API_PORT=3001
# Optional. CORS origin for frontend (default: http://localhost:3000)
CORS_ORIGIN=http://localhost:3000

# Storage Configuration
# Optional. Paths for audio and transcript file storage
AUDIO_STORAGE_PATH=./data/audio
TRANSCRIPT_STORAGE_PATH=./data/transcripts

# Background Worker Configuration
# Optional. Job polling interval in milliseconds (default: 10000)
WORKER_POLL_INTERVAL_MS=10000
# Optional. Maximum retry attempts for failed jobs (default: 3)
WORKER_MAX_RETRIES=3
# Optional. Delay between retries in milliseconds (default: 60000)
WORKER_RETRY_DELAY_MS=60000

# Logging
# Optional. Log level: debug, info, warn, error (default: info)
LOG_LEVEL=info