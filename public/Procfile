# Railway Procfile
# 定义每个服务的启动命令

# Service 1: Agent (x402 付费 API)
agent: bun run src/index.ts

# Service 2: API Server (免费 HTTP API)
api: bun run src/api/server.ts

# Service 3: Worker (后台任务处理)
worker: bun run scripts/worker.ts
